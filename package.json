{
  "name": "vite",
  "description": "Include Bootstrap's source Sass and individual JavaScript plugins with Vite.",
  "version": "0.0.0",
  "private": true,
  "stackblitz": {
    "startCommand": "npm start"
  },
  "scripts": {
    "dev": "vite --port 7000",
    "build": "vite build && node -e \"require('fs').cpSync('src/assets', 'dist/assets', {recursive: true}) && require('fs').cpSync('src/assets', 'dist/src/assets', {recursive: true})\"",
    "start": "vite --port 7000"
  },
  "devDependencies": {
    "sass": "^1.54.8",
    "vite": "^4.0.0"
  },
  "dependencies": {
    "apexcharts": "^3.37.0",
    "bootstrap": "^5.2.3",
    "jquery": "^3.6.3"
  }
}
